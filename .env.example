# Copy this file to .env and fill in once you've created a Sanity project
# (see README.md "Setting up Sanity"). Until these are set, both sites run
# entirely on their local seed data (src/sites/*/data/seed-photos.ts).

VITE_SANITY_PROJECT_ID=
VITE_SANITY_DATASET=production
